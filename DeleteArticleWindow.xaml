<Page x:Class="AuthorApp.DeleteArticleWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AuthorApp"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Видалити статтю">

    <Page.Resources>
        <ImageBrush x:Key="BackgroundImage" ImageSource="/pthotos/letajuschij-avtomobil-kadr-iz-filma-garri-potter-i-tajnaja-komnata_rect_a3bf15c82c3d4eece0da9a88e4e0e429-3080393661.jpg" Stretch="UniformToFill"/>
        <Style x:Key="CommonComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Width" Value="100"/>
        </Style>
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#D32F2F"/>
            <Setter Property="Width" Value="100"/>
        </Style>
    </Page.Resources>

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.Background>
            <StaticResource ResourceKey="BackgroundImage"/>
        </Grid.Background>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ComboBox Grid.Row="0" Grid.ColumnSpan="2" Name="BrandComboBox" Style="{StaticResource CommonComboBoxStyle}" SelectionChanged="BrandComboBox_SelectionChanged"/>
            <ComboBox Grid.Row="1" Grid.ColumnSpan="2" Name="ModelComboBox" Style="{StaticResource CommonComboBoxStyle}" SelectionChanged="ModelComboBox_SelectionChanged"/>

            <Button Grid.Row="2" Grid.Column="0" Content="Видалити" Click="DeleteArticle" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Center"/>
            <Button Grid.Row="2" Grid.Column="1" Content="Назад" Click="GoBack" Style="{StaticResource SecondaryButtonStyle}" HorizontalAlignment="Center"/>
        </Grid>
    </Grid>
</Page>